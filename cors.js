// CORS : it stands for cross origin resource sharing;

// it a machanism which uses additional headers to tell the browser wheather the specific web app can share with the another web app. but both the web app should have different origin ;
